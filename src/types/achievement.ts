export interface AchievementItem {
  id: string;
  title: string;
  event: string;
  prize?: string;
  year?: string;
  badge?: string;
  featured?: boolean;
  prominent?: boolean;
  description?: string;
}
